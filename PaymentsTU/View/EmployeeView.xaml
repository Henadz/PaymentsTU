<UserControl x:Class="PaymentsTU.View.EmployeeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:toolkit="http://schemas.microsoft.com/wpf/2008/toolkit"
             xmlns:local="clr-namespace:PaymentsTU.View"
             xmlns:viewModel="clr-namespace:PaymentsTU.ViewModel"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="500">
    <StackPanel>
        <StackPanel  VerticalAlignment="Top" Height="23" Orientation="Horizontal" Margin="5,0,5,5" >
            <Button x:Name="btnAdd" Content="Добавить" Width="100" Margin="0,0,5,0"/>
            <Button x:Name="btnDelete" Content="Удалить" Width="100" Margin="0,0,5,0"/>
            <Button x:Name="btnEdit" Content="Редактировать" Width="100" Margin="0,0,5,0"/>
            <Button x:Name="btnRefresh" Content="Обновить" Width="100"/>
        </StackPanel>
        <!--<toolkit:DataGrid ItemsSource="{Binding Items}" IsSynchronizedWithCurrentItem="True"/>-->
        <ListView Name="Directory" IsSynchronizedWithCurrentItem="True" ItemsSource="{Binding Items}" >
            <ListView.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding FullName}" VerticalAlignment="Top" />
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="Примечание: "></TextBlock>
                            <TextBlock Text="{Binding Note}" FontStyle="Italic" />
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>
